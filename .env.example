# Supabase 프로젝트 설정값 (Project Settings > API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 토스페이먼츠 TEST 클라이언트 키 (test_ck_로 시작, 공개되어도 안전)
# 시크릿 키(test_sk_)는 여기에 넣지 않는다 — Edge Function secrets에만 저장한다.
VITE_TOSS_CLIENT_KEY=
